# Core
requests>=2.31
tenacity>=8.2
python-dotenv>=1.0

# Config & validation
pydantic>=2.6
pydantic-settings>=2.2

# Database (DB-agnostic: SQLite locally, Postgres/Supabase in prod)
SQLAlchemy>=2.0
psycopg2-binary>=2.9

# LLM (provider-agnostic layer; Gemini as default MVP provider)
google-genai>=1.0

# Dashboard (>=1.50: st.fragment(run_every=...) for the payment watcher,
# width="stretch" on buttons)
streamlit>=1.50
plotly>=5.20
pandas>=2.1

# Auth + billing webhooks (Phase 1 monetization)
# Supabase Auth is called over plain REST (dashboard/supabase_auth.py) so that
# OAuth PKCE survives Streamlit reruns — no supabase-py dependency needed.
fastapi>=0.110
uvicorn>=0.27
extra-streamlit-components>=0.1.71

# Tests
pytest>=8.0

# Optional - Apple Ads search-popularity provider (VOLUME_PROVIDER=asa):
# PyJWT>=2.8
# cryptography>=42
