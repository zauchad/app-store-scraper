# Core
requests>=2.31
tenacity>=8.2
python-dotenv>=1.0

# Config & validation
pydantic>=2.6
pydantic-settings>=2.2

# Database (DB-agnostic: SQLite locally, Postgres/Supabase in prod)
SQLAlchemy>=2.0
psycopg2-binary>=2.9

# LLM (provider-agnostic layer; Gemini as default MVP provider)
google-genai>=1.0

# Dashboard
streamlit>=1.33
plotly>=5.20
pandas>=2.1

# Auth + billing webhooks (Phase 1 monetization)
supabase>=2.4
fastapi>=0.110
uvicorn>=0.27
extra-streamlit-components>=0.1.71

# Tests
pytest>=8.0

# Optional - Apple Ads search-popularity provider (VOLUME_PROVIDER=asa):
# PyJWT>=2.8
# cryptography>=42
